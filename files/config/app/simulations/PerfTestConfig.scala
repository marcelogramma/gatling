package PerfTestConfig

object PerfTestConfig {
  val baseUrl = "https://xxx.xxxxxxxxxx.xxx"
  val requestPerSecond = 100
  val durationMin = 2
  val meanResponseTimeMs = 600
  val maxResponseTimeMs = 200
}